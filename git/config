[core]
    autocrlf = input
    editor = nvim
    pager = delta

[delta]
    line-numbers = true
    syntax-theme = OneHalfLight

[interactive]
    diffFilter = delta --color-only

[pull]
    rebase = false

